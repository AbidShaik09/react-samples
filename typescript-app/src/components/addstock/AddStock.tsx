import React from 'react'
import PropTypes from 'prop-types'

const AddStock = (props:any) => {
  return (
    <div>AddStock {props.id}</div>
  )
}

AddStock.propTypes = {id:0}

export default AddStock